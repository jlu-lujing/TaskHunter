---
title: Тунелі
description: Безпечно відкрийте TaskHunter для віддаленого та мобільного доступу.
---

# Тунелі

Тунель — це публічне посилання на ваш TaskHunter, щоб дістатися до нього зі звичайного браузера в іншій мережі. Скористайтеся `taskhunter tunnel`, щоб створити його для запущеного інстансу.

> Для підключення **власних пристроїв** (мобільного застосунку, іншого десктопа) тунель зазвичай не потрібен — краще [зв'яжіть їх](/uk/connect-devices/) і дозвольте наскрізно зашифрованому [Private Relay](/uk/private-relay/) забезпечити доступ поза домом без жодних налаштувань.

## Передумови

TaskHunter запускає CLI провайдера тунелю на вашій машині. Спочатку встановіть провайдера, який хочете використовувати:

```bash
brew install cloudflared
brew install ngrok
```

Швидкі тунелі Cloudflare працюють через `cloudflared`. Для Ngrok потрібен акаунт ngrok і authtoken з dashboard ngrok:

```bash
ngrok config add-authtoken <your-ngrok-token>
```

## Швидкий старт

1. Запустіть TaskHunter:

```bash
taskhunter
```

Якщо пропустити цей крок, `taskhunter tunnel start` може автоматично запустити CLI server. Під час auto-start можна передати server options: `--port`, `--host`, `--lan`, `--ui-password` і `--api-only`.

2. Запустіть тунель Cloudflare:

```bash
taskhunter tunnel start --provider cloudflare --mode quick
```

Або запустіть тунель Ngrok:

```bash
taskhunter tunnel start --provider ngrok --mode quick
```

3. Перевірте статус:

```bash
taskhunter tunnel status
```

Коли тунель піднято, `status` показує публічний URL. Відкрийте його або відскануйте QR-код, щоб дістатися до TaskHunter звідусіль.

Типово TaskHunter виводить QR-код в інтерактивних TTY-сесіях. Використовуйте `--qr`, щоб примусово вивести QR, або `--no-qr`, щоб вимкнути його.

## Провайдери

- `cloudflare`: quick, managed remote і managed local режими
- `ngrok`: quick режим

## Керовані режими

### Кероване віддалене

Використовуйте токен + хостнейм, керовані Cloudflare:

```bash
taskhunter tunnel start --provider cloudflare --mode managed-remote --token-file ~/.secrets/cf-token --hostname app.example.com
```

### Кероване локальне

Використовуйте локальний конфіг `cloudflared`:

```bash
taskhunter tunnel start --provider cloudflare --mode managed-local --config ~/.cloudflared/config.yml
```

## Профілі (managed-remote)

Збережіть багаторазовий профіль:

```bash
taskhunter tunnel profile add --provider cloudflare --mode managed-remote --name prod-main --hostname app.example.com --token-file ~/.secrets/cf-token
```

Запуск зі збереженим профілем:

```bash
taskhunter tunnel start --profile prod-main
```

## Корисні команди

```bash
taskhunter tunnel providers
taskhunter tunnel ready --provider cloudflare
taskhunter tunnel ready --provider ngrok
taskhunter tunnel doctor --provider cloudflare
taskhunter tunnel doctor --provider ngrok
taskhunter tunnel stop --port 3000
```

## Нотатки щодо поведінки

- один активний тунель на інстанс TaskHunter (порт)
- запуск нового режиму/провайдера на тому самому інстансі замінює попередній тунель
- генерація нового посилання для підключення відкликає попереднє невикористане
- авто-старт тунелю зберігає серверні прапорці на кшталт `--ui-password` та `--api-only` у налаштуваннях інстансу, які використовуються під час рестарту/оновлення

## Пов'язане

- [Підключення пристрою](/uk/connect-devices/) — зв'яжіть власні пристрої без публічного URL
- [Безпека](/uk/security/) — захистіть інтерфейс перед відкриттям доступу
- [Десктопні тунелі](/uk/desktop-tunnels/) — налаштування тунелю в десктопному застосунку без CLI-старту
- [PWA та мобільний доступ](/uk/mobile/) — відкривайте TaskHunter з телефона
- [Усунення несправностей](/uk/troubleshooting/) — якщо посилання тунелю не працює
